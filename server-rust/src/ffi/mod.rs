//! FFI boundary for existing NanoKVM hardware libraries.
//!
//! The Rust backend must keep the current video and hardware stack. Bindings to
//! `libkvm.so` and `libkvm_mmf.so` should be added here behind narrow safe APIs.
